<!DOCTYPE html>
<html lang="pt-PT">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>POV App - Neural CRM V33.1 (Gold Master)</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #000000;
            --bg-card: #0c0c0c;
            --border-subtle: #222222;
            --gold-pov: #D4AF37;
            --gold-dim: rgba(212, 175, 55, 0.15);
            --text-main: #f0f0f0;
            --text-muted: #888888;
        }

        body { font-family: 'Montserrat', sans-serif; background-color: var(--bg-deep); color: var(--text-main); padding: 15px; margin: 0; -webkit-font-smoothing: antialiased; }
        
        .header-brand { text-align: center; margin-bottom: 25px; margin-top: 10px; }
        .header-brand h1 { font-family: 'Playfair Display', serif; color: var(--gold-pov); font-size: 24px; margin: 0; letter-spacing: 3px; font-weight: 600; text-transform: uppercase; }
        .header-brand p { font-size: 10px; color: var(--text-muted); letter-spacing: 5px; text-transform: uppercase; margin-top: 5px; }

        .card { background: var(--bg-card); padding: 25px 20px; border-radius: 2px; border: 1px solid var(--border-subtle); margin-bottom: 20px; position: relative; overflow: hidden; }
        .card::before { content: ''; position: absolute; top: 0; left: 0; width: 3px; height: 100%; background: var(--gold-pov); opacity: 0.8; }
        
        h2 { font-family: 'Playfair Display', serif; margin-top: 0; font-size: 15px; text-transform: uppercase; border-bottom: 1px solid var(--border-subtle); padding-bottom: 12px; color: var(--text-main); letter-spacing: 1px; font-weight: 400; }
        h2 span { color: var(--gold-pov); margin-right: 8px; font-size: 14px; }
        
        .btn-acao { background-color: #ffffff; color: #000; border: none; padding: 16px; font-size: 12px; font-weight: 600; border-radius: 2px; width: 100%; margin-bottom: 15px; cursor: pointer; text-transform: uppercase; letter-spacing: 1.5px; transition: 0.3s; font-family: 'Montserrat', sans-serif; }
        .btn-acao:active { transform: scale(0.98); }
        .btn-acao:disabled { background-color: #333; color: #666; cursor: not-allowed; }
        
        .btn-gold { background-color: var(--gold-pov); color: #000; box-shadow: 0 4px 15px rgba(212,175,55,0.15); border: 1px solid #ebc64f; }
        .btn-outline { background: transparent; color: var(--gold-pov); border: 1px solid var(--gold-pov); }
        .btn-guardar { display: none; margin-top: 20px; }
        
        .input-group { display: flex; gap: 10px; margin-bottom: 15px; }
        .input-meta, .select-divisao { flex: 1; padding: 14px; border-radius: 2px; border: 1px solid var(--border-subtle); background: #111; color: #fff; font-size: 13px; font-family: inherit; transition: 0.3s; }
        .input-meta:focus, .select-divisao:focus { outline: none; border-color: var(--gold-pov); background: #000; }
        
        .btn-mic-pequeno { background-color: #111; color: var(--gold-pov); border: 1px solid var(--border-subtle); border-radius: 2px; width: 48px; display: flex; justify-content: center; align-items: center; cursor: pointer; font-size: 16px; transition: 0.3s; }
        .btn-mic-pequeno.recording { background-color: #8b0000; color: #fff; border-color: #ff0000; }
        
        .hidden-input { position: absolute; width: 0.1px; height: 0.1px; opacity: 0; overflow: hidden; z-index: -1; }
        
        .upload-wrapper { background: #0a0a0a; border: 1px dashed var(--border-subtle); padding: 15px; border-radius: 2px; margin-bottom: 12px; text-align: center; transition: 0.3s; }
        .upload-wrapper.loaded { border-color: var(--gold-pov); border-style: solid; background: var(--gold-dim); }
        .lbl-doc-title { font-size: 10px; color: var(--text-muted); text-transform: uppercase; font-weight: 500; margin-bottom: 12px; display: block; letter-spacing: 1px; }
        .lbl-doc-title.loaded { color: var(--gold-pov); }
        
        .dual-btn-container { display: flex; gap: 8px; width: 100%; justify-content: center; }
        .btn-micro { background: #111; color: var(--text-main); border: 1px solid var(--border-subtle); border-radius: 2px; padding: 12px 5px; font-size: 9px; cursor: pointer; text-transform: uppercase; flex: 1; font-weight: 500; display: block; margin: 0; text-align: center; letter-spacing: 1px; transition: 0.2s;}
        .btn-micro:active { border-color: var(--gold-pov); color: var(--gold-pov); }

        /* ENGINE AI CSS */
        .ai-engine-box { border: 1px solid var(--gold-pov); background: #080808; padding: 20px; border-radius: 2px; text-align: center; margin-bottom: 15px; box-shadow: inset 0 0 20px rgba(212,175,55,0.05); }
        .ai-title { font-family: 'Playfair Display', serif; color: var(--gold-pov); font-size: 16px; margin-bottom: 5px; }
        .ai-subtitle { font-size: 9px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; margin-bottom: 20px; }
        
        #previewImagem { width: 100%; max-height: 300px; object-fit: cover; border-radius: 2px; margin-bottom: 15px; display: none; border: 1px solid var(--border-subtle); }
        
        .painel-ditado { display: none; align-items: flex-start; gap: 10px; margin-bottom: 10px; background: #0a0a0a; padding: 12px; border-radius: 2px; border: 1px solid var(--border-subtle); }
        .btn-icone-mic { background-color: #222; color: var(--gold-pov); border: 1px solid var(--border-subtle); border-radius: 50%; width: 50px; height: 50px; font-size: 18px; display: flex; justify-content: center; align-items: center; cursor: pointer; flex-shrink: 0; transition: 0.3s; }
        .btn-icone-mic.recording { background-color: #8b0000; border-color: #ff0000; color: #fff; }
        .visor-texto { width: 100%; min-height: 80px; padding: 12px; border-radius: 2px; border: 1px solid transparent; background: #111; color: #fff; font-size: 13px; resize: none; font-family: inherit; line-height: 1.5; }
        .visor-texto:focus { outline: none; border: 1px solid var(--gold-pov); }
        
        .painel-tags { display: none; flex-wrap: wrap; gap: 8px; margin-bottom: 15px; }
        .smart-tag { background: transparent; border: 1px solid var(--border-subtle); color: var(--text-muted); padding: 8px 14px; border-radius: 30px; font-size: 9px; cursor: pointer; text-transform: uppercase; letter-spacing: 1px; transition: 0.2s; font-family: inherit; }
        .smart-tag:active { border-color: var(--gold-pov); color: var(--gold-pov); background: var(--gold-dim); }
        
        .status { font-size: 9px; color: var(--text-muted); margin-bottom: 15px; text-transform: uppercase; text-align: center; font-weight: 500; letter-spacing: 2px; }
        
        #canvasAssinatura { width: 100%; height: 150px; background: #ffffff; border-radius: 2px; cursor: crosshair; touch-action: none; margin-bottom: 10px; }
        .btn-limpar-canvas { background: transparent; border: 1px solid var(--border-subtle); color: var(--text-muted); padding: 10px; font-size: 10px; text-transform: uppercase; border-radius: 2px; cursor: pointer; width: 100%; margin-bottom: 15px; letter-spacing: 1px;}
        
        .foto-item-ui { background: #0a0a0a; padding: 15px; border-radius: 2px; margin-bottom: 10px; border-left: 2px solid var(--gold-pov); display: flex; align-items: flex-start; gap: 15px; border-bottom: 1px solid var(--border-subtle); border-right: 1px solid var(--border-subtle); border-top: 1px solid var(--border-subtle);}
        .ui-img-box { width: 70px; height: 70px; border-radius: 2px; background-size: cover; background-position: center; flex-shrink: 0; border: 1px solid #333; }
        .foto-item-ui p { font-size: 11px; margin: 0; flex: 1; color: #ccc; line-height: 1.5; white-space: pre-wrap; }
        .badge-categoria { font-size: 8px; background: #111; color: var(--gold-pov); padding: 4px 8px; border-radius: 2px; text-transform: uppercase; margin-bottom: 6px; display: inline-block; letter-spacing: 1px; border: 1px solid #222;}
        .btn-dl-img { background: transparent; color: var(--text-muted); border: 1px solid var(--border-subtle); border-radius: 2px; font-size: 8px; padding: 6px 10px; cursor: pointer; margin-top: 8px; text-transform: uppercase; letter-spacing: 1px; }
        
        #pdf-render-zone { display: none; background-color: #ffffff; color: #000000; width: 100%; padding: 40px; box-sizing: border-box; margin: 0; }
        
        #crm-dashboard { display: block; }
        #app-ui-container { display: none; }
        .crm-item { background: #0a0a0a; border: 1px solid var(--border-subtle); padding: 18px; border-radius: 2px; margin-bottom: 12px; cursor: pointer; border-left: 3px solid var(--gold-pov); transition: 0.3s; display: flex; justify-content: space-between; align-items: center; }
        .crm-item:active { background: #111; }
        .crm-title { font-weight: 600; font-size: 13px; color: #fff; margin-bottom: 6px; font-family: 'Playfair Display', serif; letter-spacing: 0.5px;}
        .crm-ref { font-size: 10px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.5px; }
        .btn-voltar { background: transparent; color: var(--text-muted); border: none; padding: 10px 0; width: 100%; margin-bottom: 20px; text-transform: uppercase; font-size: 10px; cursor: pointer; letter-spacing: 2px; border-bottom: 1px solid var(--border-subtle); text-align: left;}
    </style>
</head>
<body>

    <div class="header-brand">
        <h1>POV<span>CRM</span></h1>
        <p>Neural Enterprise System</p>
    </div>

    <div id="crm-dashboard">
        <div class="card">
            <h2><span>🗂️</span> Gestor de Portfólio</h2>
            <div id="lista-crm" style="margin-bottom: 25px;"></div>
            <button id="btnNovaPasta" class="btn-acao btn-gold">➕ INICIAR NOVO MANDATO</button>
            <button id="btnLimparTudo" class="btn-acao" style="background: transparent; color: #444; border: 1px solid #222; margin-top: 10px; font-size: 9px; box-shadow: none;">FORMATAR BASE DE DADOS</button>
        </div>
    </div>

    <div id="app-ui-container">
        <button id="btnVoltarCRM" class="btn-voltar">⬅ Voltar ao Portfólio</button>
        
        <div class="card">
            <h2><span>🏢</span> 1. Identidade Corporativa</h2>
            <div class="input-group"><input type="text" id="inputAgencia" class="input-meta" placeholder="Nome da Agência Oficial" style="border-color: var(--gold-pov);"></div>
            <div class="input-group"><input type="text" id="inputAMI" class="input-meta" placeholder="Licença AMI"></div>
            
            <div class="upload-wrapper" id="wrap_logo">
                <span class="lbl-doc-title" id="lblText_logo">📄 Logótipo da Agência</span>
                <div class="dual-btn-container">
                    <label for="fileLogo_gal" class="btn-micro">📂 Ficheiros</label>
                    <input type="file" id="fileLogo_gal" class="hidden-input">
                    <label for="fileLogo_cam" class="btn-micro">📸 Câmara</label>
                    <input type="file" id="fileLogo_cam" accept="image/*" capture="environment" class="hidden-input">
                </div>
            </div>
        </div>

        <div class="card">
            <h2><span>📍</span> 2. Referência do Ativo</h2>
            <div class="input-group"><input type="text" id="inputReferencia" class="input-meta" placeholder="ID de Referência Interna"></div>
            <div class="input-group">
                <input type="text" id="inputLocal" class="input-meta" placeholder="Localização Premium">
                <button id="micLocal" class="btn-mic-pequeno">🎙️</button>
            </div>
            <div class="input-group"><input type="text" id="inputPreco" class="input-meta" placeholder="Target de Venda (€)"></div>
        </div>

        <div class="card">
            <h2><span>👤</span> 3. Entidade Proprietária</h2>
            <div class="input-group"><input type="text" id="inputNomeProp" class="input-meta" placeholder="Nome do Titular"></div>
            <div class="input-group"><input type="text" id="inputContactoProp" class="input-meta" placeholder="Contacto Direto"></div>
            <div class="input-group"><input type="text" id="inputMoradaProp" class="input-meta" placeholder="Morada Fiscal Registada"></div>
            
            <div style="display: flex; gap: 10px;">
                <div class="upload-wrapper" id="wrap_ccF" style="flex:1;">
                    <span class="lbl-doc-title" id="lblText_ccF">ID (Frente)</span>
                    <div class="dual-btn-container">
                        <label for="fileCCF_gal" class="btn-micro">📂 Doc</label>
                        <input type="file" id="fileCCF_gal" class="hidden-input">
                        <label for="fileCCF_cam" class="btn-micro">📸 Câm</label>
                        <input type="file" id="fileCCF_cam" accept="image/*" capture="environment" class="hidden-input">
                    </div>
                </div>
                <div class="upload-wrapper" id="wrap_ccV" style="flex:1;">
                    <span class="lbl-doc-title" id="lblText_ccV">ID (Verso)</span>
                    <div class="dual-btn-container">
                        <label for="fileCCV_gal" class="btn-micro">📂 Doc</label>
                        <input type="file" id="fileCCV_gal" class="hidden-input">
                        <label for="fileCCV_cam" class="btn-micro">📸 Câm</label>
                        <input type="file" id="fileCCV_cam" accept="image/*" capture="environment" class="hidden-input">
                    </div>
                </div>
            </div>
        </div>

        <div class="card">
            <h2><span>📂</span> 4. Conformidade Legal</h2>
            
            <div class="upload-wrapper" id="wrap_caderneta">
                <span class="lbl-doc-title" id="lblText_caderneta">Caderneta Predial</span>
                <div class="dual-btn-container">
                    <label for="fileCaderneta_gal" class="btn-micro">📂 Ficheiros</label>
                    <input type="file" id="fileCaderneta_gal" class="hidden-input">
                    <label for="fileCaderneta_cam" class="btn-micro">📸 Câmara</label>
                    <input type="file" id="fileCaderneta_cam" accept="image/*" capture="environment" class="hidden-input">
                </div>
            </div>
            
            <div class="upload-wrapper" id="wrap_registo">
                <span class="lbl-doc-title" id="lblText_registo">Registo Predial</span>
                <div class="dual-btn-container">
                    <label for="fileRegisto_gal" class="btn-micro">📂 Ficheiros</label>
                    <input type="file" id="fileRegisto_gal" class="hidden-input">
                    <label for="fileRegisto_cam" class="btn-micro">📸 Câmara</label>
                    <input type="file" id="fileRegisto_cam" accept="image/*" capture="environment" class="hidden-input">
                </div>
            </div>

            <div class="upload-wrapper" id="wrap_licenca">
                <span class="lbl-doc-title" id="lblText_licenca">Licença de Utilização</span>
                <div class="dual-btn-container">
                    <label for="fileLicenca_gal" class="btn-micro">📂 Ficheiros</label>
                    <input type="file" id="fileLicenca_gal" class="hidden-input">
                    <label for="fileLicenca_cam" class="btn-micro">📸 Câmara</label>
                    <input type="file" id="fileLicenca_cam" accept="image/*" capture="environment" class="hidden-input">
                </div>
            </div>

            <div class="upload-wrapper" id="wrap_certificado">
                <span class="lbl-doc-title" id="lblText_certificado">Certificado Energético</span>
                <div class="dual-btn-container">
                    <label for="fileCertificado_gal" class="btn-micro">📂 Ficheiros</label>
                    <input type="file" id="fileCertificado_gal" class="hidden-input">
                    <label for="fileCertificado_cam" class="btn-micro">📸 Câmara</label>
                    <input type="file" id="fileCertificado_cam" accept="image/*" capture="environment" class="hidden-input">
                </div>
            </div>

            <div class="upload-wrapper" id="wrap_outros">
                <span class="lbl-doc-title" id="lblText_outros">Outros Documentos</span>
                <div class="dual-btn-container">
                    <label for="fileOutros_gal" class="btn-micro">📂 Ficheiros</label>
                    <input type="file" id="fileOutros_gal" class="hidden-input">
                    <label for="fileOutros_cam" class="btn-micro">📸 Câmara</label>
                    <input type="file" id="fileOutros_cam" accept="image/*" capture="environment" class="hidden-input">
                </div>
            </div>
        </div>

        <div class="card" style="border-color: var(--gold-pov);">
            <h2><span>📷</span> 5. Motor de Vistoria Elite</h2>
            <select id="selectDivisao" class="select-divisao" style="width: 100%; margin-bottom: 20px;">
                <option value="Exterior / Fachada">Exterior / Fachada</option>
                <option value="Hall de Entrada">Hall de Entrada</option>
                <option value="Sala de Estar / Jantar">Sala de Estar / Jantar</option>
                <option value="Cozinha">Cozinha</option>
                <option value="Quarto Principal (Suite)">Quarto Principal (Suite)</option>
                <option value="Quarto">Quarto</option>
                <option value="Casa de Banho">Casa de Banho</option>
                <option value="Jardim / Piscina">Jardim / Piscina</option>
                <option value="Cave / Arrumos">Cave / Arrumos</option>
            </select>

            <div class="ai-engine-box">
                <div class="ai-title">CINEMATIC GRADING</div>
                <div class="ai-subtitle">Injeção de Luz Natural Local (Edge AI)</div>
                
                <input type="file" accept="image/*" capture="environment" id="camera-input-edge" class="hidden-input">
                <button id="btnCapturaEdge" class="btn-acao btn-gold" style="margin-bottom: 10px;">📸 CAPTURAR & VALORIZAR ATIVO</button>
                
                <input type="file" accept="image/*" id="gallery-input-edge" class="hidden-input">
                <button id="btnGaleriaEdge" class="btn-acao btn-outline" style="margin-bottom: 0; font-size: 9px; padding: 12px;">📂 PROCESSAR DA GALERIA</button>
                
                <canvas id="processing-canvas-edge" style="display: none;"></canvas>
            </div>
            
            <div class="status" id="micStatus">Aguardando Sensor Ótico...</div>
            <img id="previewImagem" src="" alt="Preview">
            
            <div class="painel-ditado" id="painelDitado">
                <button id="btnIconeMic" class="btn-icone-mic">🎙️</button>
                <div style="width: 100%;">
                    <textarea id="visorNota" class="visor-texto" placeholder="Dite a arquitetura e detalhes premium..."></textarea>
                </div>
            </div>
            
            <div class="painel-tags" id="painelTags">
                <button class="smart-tag" onclick="adicionarTag('Luz Natural Privilegiada')">Luz Natural</button>
                <button class="smart-tag" onclick="adicionarTag('Acabamentos de Alta Gama')">Alta Gama</button>
                <button class="smart-tag" onclick="adicionarTag('Design Contemporâneo')">Design Atual</button>
                <button class="smart-tag" onclick="adicionarTag('Layout Fluído')">Layout</button>
                <button class="smart-tag" onclick="adicionarTag('Áreas Generosas e Bem Distribuídas')">Áreas</button>
            </div>
            
            <button id="guardarBtn" class="btn-acao btn-gold btn-guardar">GUARDAR ESPAÇO E EXTRAIR GPS</button>
        </div>

        <div class="card">
            <h2><span>✍️</span> 6. Validação Legal</h2>
            <canvas id="canvasAssinatura"></canvas>
            <button id="btnLimparCanvas" class="btn-limpar-canvas">Limpar Quadro de Assinatura</button>
        </div>

        <div class="card">
            <h2><span>🗂️</span> Emissão C-Suite</h2>
            <div class="input-group" style="margin-bottom: 20px;">
                <input type="text" id="inputNomePdf" class="input-meta" placeholder="Nomenclatura do Relatório (Ex: POV_ID001)">
            </div>
            
            <div id="log-ui" style="font-size: 11px; color: var(--text-muted); margin-bottom: 20px;">Ainda sem registos técnicos processados.</div>
            <button id="gerarPdfBtn" class="btn-acao btn-gold">GERAR PITCH DECK DE INVESTIMENTO (PDF)</button>
        </div>
    </div>

    <div id="pdf-render-zone"></div>

    <script>
        let crmData = JSON.parse(localStorage.getItem('pov_crm_v33_master')) || [];
        let activeIndex = -1; 
        let metadados = {}; let docsApp = {}; let memoriaApp = []; let assinaturaSalvaBase64 = "";
        let tempImagemBase64 = "";

        const uiDashboard = document.getElementById('crm-dashboard');
        const uiEditor = document.getElementById('app-ui-container');
        const listaCRM = document.getElementById('lista-crm');

        function guardarNoCRM() {
            if (activeIndex === -1) return;
            crmData[activeIndex] = {
                id: crmData[activeIndex].id || new Date().getTime(),
                titulo: metadados.local || metadados.ref || "Novo Mandato " + (activeIndex + 1),
                data: crmData[activeIndex].data || new Date().toLocaleDateString('pt-PT'),
                meta: metadados, docs: docsApp, fotos: memoriaApp, assinatura: assinaturaSalvaBase64
            };
            localStorage.setItem('pov_crm_v33_master', JSON.stringify(crmData));
            renderizarDashboard();
        }

        function renderizarDashboard() {
            listaCRM.innerHTML = "";
            if(crmData.length === 0) { listaCRM.innerHTML = "<p style='color:#666; font-size:11px; text-transform: uppercase;'>Sem registos ativos no pipeline.</p>"; return; }
            
            crmData.forEach((pasta, index) => {
                const numFotos = pasta.fotos ? pasta.fotos.length : 0;
                listaCRM.innerHTML += `
                    <div class="crm-item" onclick="abrirPasta(${index})">
                        <div>
                            <div class="crm-title">${pasta.titulo}</div>
                            <div class="crm-ref">${pasta.data} | 📷 ${numFotos} Ativos Visuais</div>
                        </div>
                        <div style="color:var(--gold-pov); font-size:16px;">➔</div>
                    </div>`;
            });
        }

        const docKeys = [
            { key: 'logo', idText: 'lblText_logo', idWrap: 'wrap_logo', txt: 'Logótipo da Agência' },
            { key: 'ccF', idText: 'lblText_ccF', idWrap: 'wrap_ccF', txt: 'ID (Frente)' },
            { key: 'ccV', idText: 'lblText_ccV', idWrap: 'wrap_ccV', txt: 'ID (Verso)' },
            { key: 'caderneta', idText: 'lblText_caderneta', idWrap: 'wrap_caderneta', txt: 'Caderneta Predial' },
            { key: 'registo', idText: 'lblText_registo', idWrap: 'wrap_registo', txt: 'Registo Predial' },
            { key: 'licenca', idText: 'lblText_licenca', idWrap: 'wrap_licenca', txt: 'Licença de Utilização' },
            { key: 'certificado', idText: 'lblText_certificado', idWrap: 'wrap_certificado', txt: 'Certificado Energético' },
            { key: 'outros', idText: 'lblText_outros', idWrap: 'wrap_outros', txt: 'Outros Documentos' }
        ];

        function resetarCameraVisual() {
            tempImagemBase64 = "";
            document.getElementById('previewImagem').style.display = "none";
            document.getElementById('previewImagem').src = "";
            document.getElementById('painelDitado').style.display = "none";
            document.getElementById('painelTags').style.display = "none";
            document.getElementById('guardarBtn').style.display = "none";
            document.getElementById('visorNota').value = "";
            document.getElementById('micStatus').innerText = "Aguardando Sensor Ótico...";
            document.getElementById('micStatus').style.color = "var(--text-muted)";
        }

        function abrirPasta(index) {
            activeIndex = index;
            const pasta = crmData[index];
            metadados = pasta.meta || {}; docsApp = pasta.docs || {}; memoriaApp = pasta.fotos || []; assinaturaSalvaBase64 = pasta.assinatura || "";
            
            mapMeta.forEach(b => { const el = document.getElementById(b.id); if(el) el.value = metadados[b.key] || ""; });
            
            docKeys.forEach(d => {
                const txtEl = document.getElementById(d.idText);
                const wrapEl = document.getElementById(d.idWrap);
                if(docsApp[d.key]) {
                    txtEl.innerText = "✅ " + d.txt;
                    txtEl.classList.add('loaded'); wrapEl.classList.add('loaded');
                } else {
                    txtEl.innerText = d.txt;
                    txtEl.classList.remove('loaded'); wrapEl.classList.remove('loaded');
                }
            });

            resetarCameraVisual();
            renderizarInterface();
            ctxAssinatura.clearRect(0, 0, canvasAssinatura.width, canvasAssinatura.height);
            if(assinaturaSalvaBase64 !== "") { const img = new Image(); img.onload = () => ctxAssinatura.drawImage(img, 0, 0); img.src = assinaturaSalvaBase64; temAssinatura = true; } 
            else { temAssinatura = false; }

            uiDashboard.style.display = "none"; uiEditor.style.display = "block"; window.scrollTo(0,0);
        }

        document.getElementById('btnNovaPasta').addEventListener('click', () => {
            crmData.push({ id: new Date().getTime(), titulo: "Rascunho de Target...", data: new Date().toLocaleDateString('pt-PT'), meta: {}, docs: {}, fotos: [], assinatura: "" });
            abrirPasta(crmData.length - 1);
        });

        document.getElementById('btnVoltarCRM').addEventListener('click', () => {
            guardarNoCRM(); uiEditor.style.display = "none"; uiDashboard.style.display = "block"; window.scrollTo(0,0);
        });

        document.getElementById('btnLimparTudo').addEventListener('click', () => {
            if(confirm("DIRETIVA C-SUITE: Esta ação destruirá todo o pipeline visual e legal. Proceder?")) {
                localStorage.removeItem('pov_crm_v33_master'); crmData = []; renderizarDashboard();
            }
        });

        const mapMeta = [
            { id: 'inputAgencia', key: 'agencia' }, { id: 'inputAMI', key: 'ami' }, { id: 'inputReferencia', key: 'ref' },
            { id: 'inputLocal', key: 'local' }, { id: 'inputPreco', key: 'preco' }, { id: 'inputNomeProp', key: 'nomeProp' },
            { id: 'inputContactoProp', key: 'contactoProp' }, { id: 'inputMoradaProp', key: 'moradaProp' }
        ];

        mapMeta.forEach(b => { const el = document.getElementById(b.id); if(el) { el.addEventListener('input', (e) => { metadados[b.key] = e.target.value; guardarNoCRM(); }); }});

        function setupDualDocUpload(idGal, idCam, lblTextId, wrapId, docKey, originalText) {
            const handleFile = function(e) {
                const file = e.target.files; if(!file) return;
                if (!file.type.startsWith('image/')) { alert("⚠️ Entrada rejeitada. Formato não visual."); e.target.value = ""; return; }

                const reader = new FileReader();
                reader.onload = function(event) {
                    const img = new Image();
                    img.onload = function() {
                        const canvas = document.createElement('canvas'); 
                        let maxW = docKey === 'logo' ? 400 : 800; 
                        let w = img.width, h = img.height; if (w > maxW) { h = Math.round(h * maxW / w); w = maxW; }
                        canvas.width = w; canvas.height = h; canvas.getContext('2d').drawImage(img, 0, 0, w, h);
                        docsApp[docKey] = canvas.toDataURL('image/jpeg', 0.8); 
                        guardarNoCRM();
                        document.getElementById(lblTextId).innerText = "✅ " + originalText;
                        document.getElementById(lblTextId).classList.add('loaded'); document.getElementById(wrapId).classList.add('loaded');
                    }
                    img.src = event.target.result;
                }
                reader.readAsDataURL(file);
            };
            document.getElementById(idGal).addEventListener('change', handleFile); document.getElementById(idCam).addEventListener('change', handleFile);
        }

        setupDualDocUpload('fileLogo_gal', 'fileLogo_cam', 'lblText_logo', 'wrap_logo', 'logo', 'Logótipo Oficial');
        setupDualDocUpload('fileCCF_gal', 'fileCCF_cam', 'lblText_ccF', 'wrap_ccF', 'ccF', 'ID (Frente)');
        setupDualDocUpload('fileCCV_gal', 'fileCCV_cam', 'lblText_ccV', 'wrap_ccV', 'ccV', 'ID (Verso)');
        setupDualDocUpload('fileCaderneta_gal', 'fileCaderneta_cam', 'lblText_caderneta', 'wrap_caderneta', 'caderneta', 'Caderneta Predial');
        setupDualDocUpload('fileRegisto_gal', 'fileRegisto_cam', 'lblText_registo', 'wrap_registo', 'registo', 'Registo Predial');
        setupDualDocUpload('fileLicenca_gal', 'fileLicenca_cam', 'lblText_licenca', 'wrap_licenca', 'licenca', 'Licença de Utilização');
        setupDualDocUpload('fileCertificado_gal', 'fileCertificado_cam', 'lblText_certificado', 'wrap_certificado', 'certificado', 'Certificado Energético');
        setupDualDocUpload('fileOutros_gal', 'fileOutros_cam', 'lblText_outros', 'wrap_outros', 'outros', 'Outros Documentos');


        // ==========================================
        // MOTOR POV CINEMATIC GRADING (EDGE AI)
        // ==========================================
        
        document.getElementById('btnCapturaEdge').addEventListener('click', () => document.getElementById('camera-input-edge').click());
        document.getElementById('btnGaleriaEdge').addEventListener('click', () => document.getElementById('gallery-input-edge').click());

        const handleAIProcessing = function(event) {
            const file = event.target.files; if(!file) return;
            if (!file.type.startsWith('image/')) { alert("⚠️ Rejeitado."); return; }

            document.getElementById('micStatus').innerText = "A aplicar Matriz Cinematic Grading (Edge)...";
            document.getElementById('micStatus').style.color = "var(--gold-pov)";

            const reader = new FileReader();
            reader.onload = function(e) {
                const img = new Image();
                img.onload = function() {
                    const canvas = document.getElementById('processing-canvas-edge');
                    const ctx = canvas.getContext('2d');
                    
                    let w = img.width, h = img.height;
                    // Resolução ArchViz padrão otimizada para PDF e RAM mobile
                    if (w > 1200) { h = Math.round(h * 1200 / w); w = 1200; } 
                    canvas.width = w; canvas.height = h;
                    
                    ctx.drawImage(img, 0, 0, w, h);
                    
                    const imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);
                    const data = imageData.data;

                    // O ALGORITMO POV (Custo 0€)
                    const contrast = 1.18; // Contraste corporativo denso
                    const brightness = 10; // Compensação base
                    
                    for (let i = 0; i < data.length; i += 4) {
                        let r = data[i], g = data[i + 1], b = data[i + 2];

                        r = ((r / 255 - 0.5) * contrast + 0.5) * 255;
                        g = ((g / 255 - 0.5) * contrast + 0.5) * 255;
                        b = ((b / 255 - 0.5) * contrast + 0.5) * 255;

                        r += brightness + 22; // Injeta vermelho (Golden Hour)
                        g += brightness + 12; // Injeta verde
                        b += brightness - 5;  // Elimina tons frios

                        data[i] = r > 255 ? 255 : (r < 0 ? 0 : r);
                        data[i + 1] = g > 255 ? 255 : (g < 0 ? 0 : g);
                        data[i + 2] = b > 255 ? 255 : (b < 0 ? 0 : b);
                    }

                    ctx.putImageData(imageData, 0, 0);
                    concluirCapturaVistoria(canvas.toDataURL('image/jpeg', 0.85));
                    
                    // Cleanup memory
                    ctx.clearRect(0,0, canvas.width, canvas.height);
                    event.target.value = ""; 
                }
                img.src = e.target.result;
            }
            reader.readAsDataURL(file);
        };

        document.getElementById('camera-input-edge').addEventListener('change', handleAIProcessing);
        document.getElementById('gallery-input-edge').addEventListener('change', handleAIProcessing);

        function concluirCapturaVistoria(base64Image) {
            tempImagemBase64 = base64Image; document.getElementById('previewImagem').src = tempImagemBase64;
            document.getElementById('previewImagem').style.display = "block"; document.getElementById('painelDitado').style.display = "flex";
            document.getElementById('painelTags').style.display = "flex"; document.getElementById('visorNota').value = "";
            document.getElementById('micStatus').innerText = "Processamento Edge Concluído. Extraia a metadata.";
            document.getElementById('micStatus').style.color = "var(--text-muted)";
            document.getElementById('guardarBtn').style.display = "block";
        }

        function adicionarTag(textoTag) { let textoAtual = document.getElementById('visorNota').value.trim(); let novaLinha = textoAtual.length > 0 ? '\n' : ''; document.getElementById('visorNota').value = textoAtual + novaLinha + '✦ ' + textoTag; }

        window.descarregarFoto = function(indexFoto) {
            if(activeIndex === -1 || !memoriaApp[indexFoto]) return;
            const item = memoriaApp[indexFoto]; const a = document.createElement('a'); a.href = item.imagem;
            a.download = `POV_Render_${item.categoria.replace(/[^a-zA-Z0-9]/g, '_')}_${indexFoto+1}.jpg`;
            document.body.appendChild(a); a.click(); document.body.removeChild(a);
        };

        function renderizarInterface() {
            const logUi = document.getElementById('log-ui');
            if(memoriaApp.length === 0) { logUi.innerHTML = "Ainda sem registos nesta pasta."; return; }
            logUi.innerHTML = "";
            memoriaApp.forEach((item, index) => {
                let txt = item.texto.length > 0 ? item.texto : "Sem notas estruturais.";
                logUi.innerHTML += `
                <div class="foto-item-ui">
                    <div class="ui-img-box" style="background-image: url('${item.imagem}');"></div>
                    <div style="flex:1;">
                        <div class="badge-categoria">${item.categoria}</div><p>${txt}</p>
                        <button class="btn-dl-img" onclick="descarregarFoto(${index})">⬇ Descarregar Mídia Valorizada</button>
                    </div>
                </div>`;
            });
        }

        document.getElementById('guardarBtn').addEventListener('click', function() {
            this.style.display = "none"; 
            const concluirGuardar = (coordsText) => {
                memoriaApp.push({ categoria: document.getElementById('selectDivisao').value, imagem: tempImagemBase64, texto: document.getElementById('visorNota').value.trim(), coordenadas: coordsText, dataHora: new Date().toLocaleString('pt-PT') });
                guardarNoCRM(); renderizarInterface();
                resetarCameraVisual();
                document.getElementById('micStatus').innerText = "Registo acoplado à base de dados.";
            };
            document.getElementById('micStatus').innerText = "A calcular telemetria GPS...";
            if (navigator.geolocation) { navigator.geolocation.getCurrentPosition( (pos) => { concluirGuardar(`LAT: ${pos.coords.latitude.toFixed(5)} | LON: ${pos.coords.longitude.toFixed(5)}`); }, (err) => { concluirGuardar("Sinal GPS Omitido"); }, { enableHighAccuracy: true, timeout: 5000 } );
            } else { concluirGuardar("GPS Incompatível."); }
        });

        // ASSINATURA C-SUITE
        const canvasAssinatura = document.getElementById('canvasAssinatura'); const ctxAssinatura = canvasAssinatura.getContext('2d');
        let assinando = false; let temAssinatura = false; 
        function inicializarCanvas() { const r = canvasAssinatura.parentElement.getBoundingClientRect(); canvasAssinatura.width = r.width > 0 ? r.width - 40 : window.innerWidth - 70; canvasAssinatura.height = 150; ctxAssinatura.lineWidth = 2; ctxAssinatura.lineCap = 'round'; ctxAssinatura.strokeStyle = '#000'; }
        inicializarCanvas();
        function getPos(e) { const r = canvasAssinatura.getBoundingClientRect(); const cx = e.touches ? e.touches.clientX : e.clientX; const cy = e.touches ? e.touches.clientY : e.clientY; return { x: cx - r.left, y: cy - r.top }; }
        canvasAssinatura.addEventListener('mousedown', (e)=>{ assinando=true; const p=getPos(e); ctxAssinatura.beginPath(); ctxAssinatura.moveTo(p.x, p.y); });
        canvasAssinatura.addEventListener('mousemove', (e)=>{ if(assinando){ const p=getPos(e); ctxAssinatura.lineTo(p.x, p.y); ctxAssinatura.stroke(); temAssinatura=true; }});
        canvasAssinatura.addEventListener('mouseup', ()=>{ assinando=false; if(temAssinatura) { assinaturaSalvaBase64=canvasAssinatura.toDataURL('image/png'); guardarNoCRM(); } });
        canvasAssinatura.addEventListener('touchstart', (e)=>{ e.preventDefault(); assinando=true; const p=getPos(e); ctxAssinatura.beginPath(); ctxAssinatura.moveTo(p.x, p.y); },{passive:false});
        canvasAssinatura.addEventListener('touchmove', (e)=>{ e.preventDefault(); if(assinando){ const p=getPos(e); ctxAssinatura.lineTo(p.x, p.y); ctxAssinatura.stroke(); temAssinatura=true; }},{passive:false});
        canvasAssinatura.addEventListener('touchend', ()=>{ assinando=false; if(temAssinatura) { assinaturaSalvaBase64=canvasAssinatura.toDataURL('image/png'); guardarNoCRM(); } });
        document.getElementById('btnLimparCanvas').addEventListener('click', () => { ctxAssinatura.clearRect(0, 0, canvasAssinatura.width, canvasAssinatura.height); temAssinatura = false; assinaturaSalvaBase64 = ""; guardarNoCRM(); });

        // DITADO
        const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
        function ativarDitado(inputId, btnId) {
            if (!SpeechRecognition) return;
            const rec = new SpeechRecognition(); rec.continuous = false; rec.lang = 'pt-PT';
            const target = document.getElementById(inputId); const btn = document.getElementById(btnId);
            rec.onstart = () => { btn.classList.add('recording'); };
            rec.onresult = (e) => { 
                let tEx = target.value.trim(); let novo = e.results[e.resultIndex].transcript.trim(); target.value = tEx.length > 0 ? tEx + " " + novo : novo;
                if(inputId === 'visorNota' && tempImagemBase64 !== "") document.getElementById('guardarBtn').style.display = "block";
                if(inputId === 'inputLocal') { metadados.local = target.value; guardarNoCRM(); }
            };
            rec.onend = () => { btn.classList.remove('recording'); }; try { rec.start(); } catch(e) {}
        }
        document.getElementById('micLocal').addEventListener('click', () => ativarDitado('inputLocal', 'micLocal'));
        document.getElementById('btnIconeMic').addEventListener('click', () => ativarDitado('visorNota', 'btnIconeMic'));

        // ==========================================
        // GERAÇÃO DO PITCH DECK DE INVESTIMENTO (PDF)
        // ==========================================
        document.getElementById('gerarPdfBtn').addEventListener('click', function() {
            const btn = this; btn.innerText = "⏳ A COMPILAR PITCH DECK... (Aguarde)"; btn.disabled = true;

            let nomeFicheiroFinal = document.getElementById('inputNomePdf') ? document.getElementById('inputNomePdf').value.trim() : "";
            if (nomeFicheiroFinal === "") nomeFicheiroFinal = `POV_PitchDeck_${metadados.ref || new Date().getTime()}.pdf`;
            if (!nomeFicheiroFinal.toLowerCase().endsWith('.pdf')) nomeFicheiroFinal += '.pdf';

            const pdfZone = document.getElementById('pdf-render-zone'); pdfZone.style.display = 'block';

            let htmlString = `
                <div style="font-family: 'Montserrat', sans-serif; color: #000; width: 100%; box-sizing: border-box; margin: 0;">
                    <table style="width: 100%; border-bottom: 2px solid #d4af37; padding-bottom: 15px; margin-bottom: 20px;">
                        <tr>
                            <td style="width: 50%; vertical-align: middle;">
                                ${docsApp.logo ? `<img src="${docsApp.logo}" style="max-height: 50px; max-width: 250px;">` : `<h1 style="margin:0; font-family: 'Playfair Display', serif; font-size: 24px; color:#111;">PITCH DECK DE INVESTIMENTO</h1>`}
                            </td>
                            <td style="width: 50%; text-align: right; vertical-align: middle;">
                                <div style="font-weight: 700; font-size: 16px; color: #111;">${metadados.agencia || "Entidade Não Definida"}</div>
                                <div style="font-size: 10px; color: #666; margin-top: 4px;">REGISTO OFICIAL: ${metadados.ami || "N/A"}</div>
                            </td>
                        </tr>
                    </table>
                    <table style="width: 100%; margin-bottom: 25px; table-layout: fixed; border-collapse: collapse;">
                        <tr>
                            <td style="width: 50%; vertical-align: top; padding-right: 10px;">
                                <div style="background: #fafafa; padding: 15px; border: 1px solid #eee; border-left: 3px solid #d4af37; min-height: 110px;">
                                    <div style="font-weight: 700; margin-bottom: 10px; text-transform: uppercase; font-size: 10px; color: #888; letter-spacing: 1px;">Telemetria do Ativo</div>
                                    <div style="font-size: 12px; margin-bottom: 6px;"><b style="color:#111;">Referência Interna:</b> ${metadados.ref || "N/A"}</div>
                                    <div style="font-size: 12px; margin-bottom: 6px;"><b style="color:#111;">Localização Premium:</b> ${metadados.local || "N/A"}</div>
                                    <div style="font-size: 12px;"><b style="color:#111;">Target de Posicionamento:</b> ${metadados.preco || "Sob Consulta"}</div>
                                </div>
                            </td>
                            <td style="width: 50%; vertical-align: top; padding-left: 10px;">
                                 <div style="background: #fafafa; padding: 15px; border: 1px solid #eee; border-left: 3px solid #d4af37; min-height: 110px;">
                                    <div style="font-weight: 700; margin-bottom: 10px; text-transform: uppercase; font-size: 10px; color: #888; letter-spacing: 1px;">Entidade Proprietária</div>
                                    <div style="font-size: 12px; margin-bottom: 6px;"><b style="color:#111;">Titular:</b> ${metadados.nomeProp || "N/A"}</div>
                                    <div style="font-size: 12px; margin-bottom: 6px;"><b style="color:#111;">Contacto Direto:</b> ${metadados.contactoProp || "N/A"}</div>
                                    <div style="font-size: 12px;"><b style="color:#111;">Morada Fiscal:</b> ${metadados.moradaProp || "N/A"}</div>
                                </div>
                            </td>
                        </tr>
                    </table>
                    <div style="margin-bottom: 30px; border: 1px solid #e0e0e0; padding: 15px; background: #fff;">
                        <div style="font-weight: 700; text-transform: uppercase; font-size: 11px; margin-bottom: 12px; color: #111; border-bottom: 1px solid #ccc; padding-bottom: 5px;">Auditoria de Conformidade Legal</div>
                        <table style="width: 100%; font-size: 11px; color: #444;">
                            <tr><td style="width: 50%; padding: 4px 0;">Cartão de Cidadão: <b style="color:${docsApp.ccF ? '#27ae60' : '#d35400'};">${docsApp.ccF ? 'Validado' : 'Pendente'}</b></td><td style="width: 50%; padding: 4px 0;">Caderneta Predial: <b style="color:${docsApp.caderneta ? '#27ae60' : '#d35400'};">${docsApp.caderneta ? 'Validado' : 'Pendente'}</b></td></tr>
                            <tr><td style="width: 50%; padding: 4px 0;">Registo Predial: <b style="color:${docsApp.registo ? '#27ae60' : '#d35400'};">${docsApp.registo ? 'Validado' : 'Pendente'}</b></td><td style="width: 50%; padding: 4px 0;">Licença de Utilização: <b style="color:${docsApp.licenca ? '#27ae60' : '#d35400'};">${docsApp.licenca ? 'Validado' : 'Pendente'}</b></td></tr>
                            <tr><td style="width: 50%; padding: 4px 0;">Certificado Energético: <b style="color:${docsApp.certificado ? '#27ae60' : '#d35400'};">${docsApp.certificado ? 'Validado' : 'Pendente'}</b></td><td style="width: 50%; padding: 4px 0;">Outros Documentos: <b style="color:${docsApp.outros ? '#27ae60' : '#888'};">${docsApp.outros ? 'Anexado' : 'N/A'}</b></td></tr>
                        </table>
                    </div><div style="page-break-before: auto;"></div>`;

            if(memoriaApp.length > 0) { htmlString += `<div style="font-weight: 700; font-family: 'Playfair Display', serif; text-transform: uppercase; font-size: 16px; margin-bottom: 20px; color: #d4af37; border-bottom: 1px solid #d4af37; padding-bottom: 8px;">Mapeamento Visual & Arquitetónico</div>`; }
            memoriaApp.forEach((item, index) => {
                let textoFormatado = item.texto.replace(/\n/g, '<br>'); if(textoFormatado === "") textoFormatado = "<span style='color:#999; font-style:italic;'>Sem dados estruturais registados.</span>";
                htmlString += `
                <table style="width: 100%; table-layout: fixed; border-collapse: collapse; page-break-inside: avoid; margin-bottom: 25px; border-bottom: 1px solid #f0f0f0; padding-bottom: 15px;">
                    <tr><td style="width: 50%; vertical-align: top; padding: 0;"><div style="width: 100%; height: 200px; border: 1px solid #ddd; background-color: #ececec; background-image: url('${item.imagem}'); background-size: cover; background-position: center;"></div></td><td style="width: 4%; padding: 0;"></td>
                        <td style="width: 46%; vertical-align: top; padding: 0; padding-top: 5px; box-sizing: border-box;">
                            <div style="border-bottom: 1px solid #e0e0e0; padding-bottom: 5px; margin-bottom: 8px; display: block;"><span style="font-family: 'Montserrat', sans-serif; font-size: 10px; font-weight: 600; color: #d4af37; text-transform: uppercase; letter-spacing: 2px; float: left;">${item.categoria}</span><span style="font-family: 'Montserrat', sans-serif; font-size: 10px; color: #999; float: right;">REF. 0${index + 1}</span><div style="clear: both;"></div></div>
                            <div style="font-family: 'Playfair Display', serif; font-size: 12px; color: #222; line-height: 1.6; margin: 0; min-height: 100px; width: 100%; box-sizing: border-box; white-space: pre-wrap; word-wrap: break-word;">${textoFormatado}</div>
                            <div style="margin-top: 10px; font-size: 8px; color: #888; font-family: monospace; border-top: 1px dotted #ccc; padding-top: 5px;">⏳ DATA: ${item.dataHora}<br>🛰️ COORDENADAS: ${item.coordenadas}</div>
                        </td></tr></table>`;
            });

            if (temAssinatura && assinaturaSalvaBase64 !== "") { htmlString += `<div style="display: block; width: 100%; clear: both; page-break-inside: avoid; margin-top: 30px; text-align: center; border-top: 1px solid #e0e0e0; padding-top: 30px;"><img src="${assinaturaSalvaBase64}" style="max-width: 250px; max-height: 100px; display: block; margin: 0 auto; border-bottom: 1px solid #111; padding-bottom: 5px;"><div style="font-family: 'Montserrat', sans-serif; font-size: 11px; color: #666; text-transform: uppercase; letter-spacing: 1px; margin-top: 10px;">Aprovação Executiva - Titular do Ativo</div></div>`; }
            let fotosAnexosHTML = ""; const dictAnexos = { 'ccF': 'ID Corporativo (Frente)', 'ccV': 'ID Corporativo (Verso)', 'caderneta': 'Certidão Predial', 'registo': 'Registo Predial', 'licenca': 'Licença Camarária', 'certificado': 'Eficiência Energética', 'outros': 'Anexos Suplementares' };
            for (const [key, label] of Object.entries(dictAnexos)) { if(docsApp[key]) { fotosAnexosHTML += `<div style="page-break-inside: avoid; margin-bottom: 30px; border: 1px solid #e0e0e0; padding: 15px; background: #fff;"><div style="font-weight: 700; font-size: 12px; color: #111; margin-bottom: 15px; text-transform: uppercase; border-bottom: 1px solid #d4af37; padding-bottom: 5px; display: inline-block;">📄 Anexo Oficial: ${label}</div><img src="${docsApp[key]}" style="max-width: 100%; max-height: 600px; display: block; margin: 0 auto;"></div>`; } }
            if(fotosAnexosHTML !== "") { htmlString += `<div style="page-break-before: always; padding-top: 20px;"></div><div style="font-weight: 700; font-family: 'Playfair Display', serif; text-transform: uppercase; font-size: 16px; margin-bottom: 20px; color: #d4af37; border-bottom: 1px solid #d4af37; padding-bottom: 8px;">Dossiê Documental Confidencial</div>${fotosAnexosHTML}`; }
            htmlString += `</div>`; 
            
            pdfZone.innerHTML = htmlString; 
            window.scrollTo(0, 0);

            setTimeout(() => {
                const opt = { 
                    margin:, 
                    filename: nomeFicheiroFinal, 
                    image: { type: 'jpeg', quality: 0.95 }, 
                    html2canvas: { scale: 1.5, useCORS: true, letterRendering: true, scrollY: 0 }, 
                    jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' } 
                };
                
                html2pdf().set(opt).from(pdfZone).save().then(() => { 
                    pdfZone.style.display = 'none'; 
                    pdfZone.innerHTML = ''; 
                    btn.innerText = "GERAR PITCH DECK DE INVESTIMENTO (PDF)"; 
                    btn.disabled = false;
                }).catch(err => {
                    alert("Aviso Técnico: Overflow de memória. Limpe a cache do dispositivo."); 
                    btn.innerText = "GERAR PITCH DECK DE INVESTIMENTO (PDF)"; 
                    btn.disabled = false;
                });
            }, 1000); 
        });

        renderizarDashboard();
    </script>
</body>
</html>
